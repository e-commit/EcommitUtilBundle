    /**
     * Remove {{ variableName }}.
     *
     * @param {{ variableType }} ${{ variableName }}
     */
    public function {{ methodName }}({{ variableTypeHint }} ${{ variableName }})
    {
        if ($this->{{ fieldName }}->contains(${{ variableName }})) {
            $this->{{ fieldName }}->removeElement(${{ variableName }});
        }
    }
